"use client"

import PasswordSetup from "@/components/password-setup/PasswordSetup"

/**
 * Backward-compatible password setup route.
 * @returns {JSX.Element}
 */
export default function SetupPasswordPage() {
  return <PasswordSetup />
}
