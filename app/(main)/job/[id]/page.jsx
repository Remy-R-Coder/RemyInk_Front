"use client"

import Job from "@/components/job/Job"

/**
 * Job detail page (singular route compatibility)
 * Supports links targeting /job/:id
 * @returns {JSX.Element}
 */
export default function JobDetailCompatPage() {
  return <Job />
}

